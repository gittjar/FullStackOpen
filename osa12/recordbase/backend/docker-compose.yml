version: '3'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - mongo
    environment:
      - MONGO_URL=mongodb://mongo:27017/recordbase
  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"